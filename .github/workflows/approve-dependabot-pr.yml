name: "Approve Dependabot PR's" 
on:
  pull_request:

permissions:
  contents: write
  pull-requests: write
  actions: write
  
jobs:
  dependabot:
    uses: devops-actions/.github/.github/workflows/approve-dependabot-pr.yml@5e1e286077541a5a9a984dbd52df15ba382ca025 # main
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
